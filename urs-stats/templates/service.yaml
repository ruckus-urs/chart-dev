apiVersion: v1
kind: Service
metadata:
  labels:
    app: urs-stats
  name: urs-stats
spec:
  ports:
    - port: 8020
      protocol: TCP
      targetPort: 8020
    - port: 8443
      protocol: TCP
      targetPort: 8443
  selector:
    app: urs-stats
  type: ClusterIP

